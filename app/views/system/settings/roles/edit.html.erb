<%- content_for :title do %>
  Edit role
<% end %>
<%- content_for :right_bar do %>
  <div class="card">
    <div class="card-header">
      <strong class="card-title">Edit role</strong>
    </div>
    <div class="card-body">
      <div class="card-body">
        <%= render 'shared/flash' unless flash.empty? %>
        <%= render 'system/settings/roles/shared/form', btn_label: 'Edit' %>
      </div>
    </div>
  </div>
<% end %>